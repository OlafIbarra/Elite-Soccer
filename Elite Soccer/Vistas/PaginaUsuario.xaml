<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pancake="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
             x:Class="Elite_Soccer.Vistas.PaginaUsuario"
             BackgroundColor="#111111"
             NavigationPage.HasNavigationBar="False">

    <Grid RowDefinitions="Auto,*">

        <!-- Encabezado fijo -->
        <StackLayout Grid.Row="0" BackgroundColor="#111111" Spacing="10" Padding="10">

            <!-- Logo + botón salir -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Logo en círculo -->
                <Frame BackgroundColor="White"
                       Padding="10"
                       CornerRadius="100"
                       HeightRequest="80"
                       WidthRequest="80"
                       HorizontalOptions="Center"
                       VerticalOptions="Center"
                       Grid.ColumnSpan="2"
                       HasShadow="False">
                    <Image Source="LOGO1.png"
                           Aspect="AspectFill"
                           HeightRequest="90"
                           WidthRequest="90"/>
                </Frame>

                <!-- Botón cerrar sesión -->
                <Image Source="logout.png"
                       HeightRequest="35"
                       WidthRequest="35"
                       HorizontalOptions="End"
                       VerticalOptions="Start">
                    <Image.GestureRecognizers>
                        <TapGestureRecognizer Tapped="CerrarSesion_Clicked"/>
                    </Image.GestureRecognizers>
                </Image>
            </Grid>

            <!-- Imagen de bienvenida -->
            <Frame HasShadow="False" Padding="0" CornerRadius="20">
                <Image Source="bienvenida.jpg" Aspect="AspectFill" HeightRequest="180"/>
            </Frame>

            <!-- Texto de bienvenida -->
            <Label Text="¡Bienvenido a Club Elite!"
                   FontSize="24"
                   TextColor="Gold"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"/>
        </StackLayout>

        <!-- Contenido con scroll -->
        <ScrollView Grid.Row="1">
            <StackLayout Padding="20" Spacing="20">

                <!-- Goleadores -->
                <pancake:PancakeView BackgroundColor="#1A1A1A" CornerRadius="20" Padding="15">
                    <Grid>
                        <Grid.GestureRecognizers>
                            <TapGestureRecognizer Tapped="VerGoleadores_Clicked"/>
                        </Grid.GestureRecognizers>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Image Source="icono_goleador.png" HeightRequest="60" WidthRequest="60"/>
                        <StackLayout Grid.Column="1" Padding="10,0" VerticalOptions="Center">
                            <Label Text="Goleadores" FontSize="18" TextColor="White" FontAttributes="Bold"/>
                            <Label Text="Revisa los máximos anotadores" TextColor="Gray" FontSize="14"/>
                        </StackLayout>
                    </Grid>
                </pancake:PancakeView>

                <!-- Tabla de posiciones -->
                <pancake:PancakeView BackgroundColor="#1A1A1A" CornerRadius="20" Padding="15">
                    <Grid>
                        <Grid.GestureRecognizers>
                            <TapGestureRecognizer Tapped="VerTabla_Clicked"/>
                        </Grid.GestureRecognizers>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Image Source="icono_tabla.png" HeightRequest="60" WidthRequest="60"/>
                        <StackLayout Grid.Column="1" Padding="10,0" VerticalOptions="Center">
                            <Label Text="Tabla de posiciones" FontSize="18" TextColor="White" FontAttributes="Bold"/>
                            <Label Text="Consulta la tabla actual por categoría" TextColor="Gray" FontSize="14"/>
                        </StackLayout>
                    </Grid>
                </pancake:PancakeView>

                <!-- Calendario -->
                <pancake:PancakeView BackgroundColor="#1A1A1A" CornerRadius="20" Padding="15">
                    <Grid>
                        <Grid.GestureRecognizers>
                            <TapGestureRecognizer Tapped="VerCalendario_Clicked"/>
                        </Grid.GestureRecognizers>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Image Source="icono_calendario.png" HeightRequest="60" WidthRequest="60"/>
                        <StackLayout Grid.Column="1" Padding="10,0" VerticalOptions="Center">
                            <Label Text="Calendario de partidos" FontSize="18" TextColor="White" FontAttributes="Bold"/>
                            <Label Text="Partidos de esta semana y próximos" TextColor="Gray" FontSize="14"/>
                        </StackLayout>
                    </Grid>
                </pancake:PancakeView>

            </StackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
