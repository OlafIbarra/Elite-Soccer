<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pancake="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
             x:Class="Elite_Soccer.Vistas.GoleadoresPage"
             BackgroundColor="#111111"
             NavigationPage.HasNavigationBar="False">

    <ScrollView>
        <StackLayout Padding="20" Spacing="20" HeightRequest="1000">

            <!-- Botón de regreso -->
            <Image Source="volver.png"
                   HeightRequest="30"
                   WidthRequest="30"
                   HorizontalOptions="Start">
                <Image.GestureRecognizers>
                    <TapGestureRecognizer Tapped="Volver_Clicked"/>
                </Image.GestureRecognizers>
            </Image>

            <Label Text="GOLEADORES"
                   FontSize="22"
                   TextColor="Gold"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"/>

            <!-- Botones de categoría -->
            <Frame CornerRadius="20" BackgroundColor="Transparent" Padding="5" Margin="5" x:Name="frameCategorias">
                <Grid ColumnDefinitions="*,*">
                    <Button x:Name="btnFemenil" Text="FEMENIL" Clicked="BtnCategoria_Clicked"
                            BackgroundColor="#252525" TextColor="Gold" HeightRequest="40"/>
                    <Button x:Name="btnVaronil" Text="VARONIL" Grid.Column="1" Clicked="BtnCategoria_Clicked"
                            BackgroundColor="#1E1E1E" TextColor="White" HeightRequest="40"/>
                </Grid>
            </Frame>

            <!-- Lista de goleadores -->
            <StackLayout x:Name="contenedorGoleadores" Spacing="15"/>

           

        </StackLayout>
    </ScrollView>
</ContentPage>
